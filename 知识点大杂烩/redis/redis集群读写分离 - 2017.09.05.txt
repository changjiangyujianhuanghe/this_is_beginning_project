从节点执行readonly命令可以增加读权限

首先看下为什么要实现读写分离

1. 读写分离应用于：减轻数据库压力的情况下，大量的读请求会导致数据库访问过慢从而导致

   数据库成为整个系统的性能瓶颈；增加redis内存服务器就是为了减轻数据库的读压力

2. 通过网络连接访问redis内存中的数据比访问本地数据库还快；而且redis还可以做水平

   扩展，通过集群扩大缓存容量，缓解单点的缓存访问压力（把槽放到新节点上），一般使用redis就是为了它的读写性能，对安全要求高的数据不要放到redis中使用，避免出现

   信息丢失

 

